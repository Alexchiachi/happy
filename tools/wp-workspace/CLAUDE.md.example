# daoissimple.com · 網站內容維運

> 做幸福的事，讓幸福變成有價值的事。

這個資料夾是用來維護 **daoissimple.com**（WordPress，Blocksy 主題，cPanel 共享主機）的
**內容與文案**。程式碼與主題開發不在這裡，在 GitHub 的 `alexchiachi/happy`。

## 工作方式

網站內容透過 `./wp.py` 拉到 `content/` 變成檔案，改完再推回去。

```bash
./wp.py list pages            # 先看有哪些頁面
./wp.py pull pages 12         # 拉一頁下來
# 改 content/pages-12-*.html（內文）
# 改 content/pages-12-*.json（title 標題、excerpt 摘要）
./wp.py diff pages 12         # 推之前一定先看差異
./wp.py push pages 12 --yes   # 確認沒問題才上線
```

新增文章：`./wp.py new posts "標題" --yes` 建立草稿，寫完再 `push`。

**這個站沒有 staging 測試站。** 所以：

- `push` 之前一定先 `diff`，把改了什麼講給我聽，我確認了再推
- 一次改一頁。不要為了效率一口氣推五頁
- `content/_backup/` 是自動存的推送前快照，不要刪

**不要用 `draft` 當預覽。** 已發布的頁面設成草稿，是把它從網站上撤下來，
不是做預覽版——WordPress 沒有「已發布頁面的草稿版」。`draft` 只用在
還沒發布過的新文章上。

**不要改 `.json` 裡底線開頭的欄位。** 尤其是 `_slug`：改網址會讓所有
既有連結失效。真要改網址,請叫我停下來,那要到後台做並設轉址。

## 品牌禁區（比什麼都重要）

這些是刻意的設計選擇，不是還沒做的待辦。**不要「順手優化」掉：**

- **沒有 banner 輪播、沒有彈窗、沒有閃爍特價。** 不要因為「轉換率」把這些加回來
- **留白佔比超過 60%。** 版面看起來「空」是對的，不要為了多塞東西壓縮間距
- **雲南與台灣是對稱並列的雙翼**（雲南＝茶褐，台灣＝苔綠）。不要讓其中一邊變成主、另一邊變成附屬
- **印章元素點到為止。** 每頁底部有一個就夠，中段不要再加東方裝飾
- **動效以「呼吸感」為目標。** 慢，是這個品牌的主張

## 文案語氣

- 繁體中文。用字克制，不用驚嘆號堆疊，不用「超值」「限時」「錯過可惜」這類促銷語
- 說具體的事：地點、節氣、人的手做了什麼。不要說抽象的好
- 產地故事優先於功效宣稱。**尤其不要寫療效**（天麻相關內容特別注意，那有法規風險）
- 英文副標維持 Cormorant Garamond 的調性：簡短、不喊口號

## 文章分類（只有這四種）

生活提案 ／ 產地故事 ／ 選物筆記 ／ 二地對話

要新增分類先問過，不要自己開。

## 設計系統參考值

改內容時如果要用到顏色，只從這裡挑：

| 用途 | 色碼 |
| --- | --- |
| 紙底 paper | `#FAF6EF` |
| 墨字 ink | `#2A2520` |
| 茶褐 tea（雲南） | `#8B6F47` |
| 苔綠 moss（台灣） | `#5C7A5A` |
| 印朱 seal（落款） | `#A8543A` |

字型：標題 Cormorant Garamond，中文 Noto Serif TC。

## 不要碰的東西

- **不要改主題檔案。** Blocksy 是市售主題，直接改檔案下次更新就被蓋掉。要改外觀請開子主題，而且那屬於程式碼工作，回 `alexchiachi/happy` 做
- **不要在內容裡寫 `style=""` 調間距。** 間距由主題和設計系統處理，硬寫會在手機上跑掉
- **不要動外掛、佈景、使用者設定。** 這支工具也刻意做不到
- **不要讀 `.env`。** 那裡面是密碼

## 圖片

圖片走 WordPress 媒體庫，不從這裡上傳。要換圖的話請告訴我要換哪張、換成什麼，
我在後台上傳後把網址給你，你再寫進內容裡。

建議規格：寬度 1600px 以內，JPG，壓到 300KB 以下。
風格偏自然光、低飽和、有空氣感，避免商業過修圖。
